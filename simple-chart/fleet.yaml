helm:
  chart: config-chart
  values:
    name: example-value
  templateValues:
    {{- if $ingress :=  -}}
    url: {{ (index (lookup "v1" "Service" "kube-system" "traefik").status.loadBalancer.ingress 0).ip }}
    {{- else -}}
    url: foo
    {{- end -}}
