helm:
  chart: config-chart
  values:
    name: example-value
  templateValues:
    {{- $ingress := lookup "v1" "Service" "kube-system" "traefik" }}
    {{- if $ingress }}
    url: {{ (index $ingress.status.loadBalancer.ingress 0).ip }}
    {{- else }}
    url: foo
    {{- end }}
