helm:
  chart: config-chart
  values:
    name: example-value
  templateValues:
    {{- if eq (index .ClusterLabels "fleet.com/somevalue") "true" -}}
    url: "${ .ClusterValues.global.proxy.url }"
    {{- end -}}
