helm:
  chart: config-chart
  values:
    name: example-value
  templateValues:
    {{- if $ingress := lookup "v1" "Service" "kube-system" "traefik" -}}
    url: "${ (index $ingress.status.loadBalancer.ingress 0).ip }"
    {{- else -}}
    url: foo
    {{- end -}}
